import '../../core/tools/tool_definition.dart';

// TODO(agent): register the Workspaces feature's tools, landing and commands.
final FeatureModule workspacesModule = FeatureModule(id: 'workspaces');
