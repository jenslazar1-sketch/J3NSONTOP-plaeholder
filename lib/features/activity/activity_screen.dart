import 'package:flutter/material.dart';

// TODO(agent): replace with the full activity log screen.
class ActivityScreen extends StatelessWidget {
  const ActivityScreen({super.key});

  @override
  Widget build(BuildContext context) => const Center(child: Text('Activity'));
}
